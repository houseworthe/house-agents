# Python virtual environment
venv/
__pycache__/
*.pyc

# Test repositories (too large for git)
repos/

# Test results (may contain sensitive data)
results/*.txt
results/*.json

# Keep the results directory but not the files
!results/.gitkeep
